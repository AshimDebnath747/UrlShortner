<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>URL shortener</h1>
    <div>
        <form action="/url" method = "post">
            <label for="">Enter your RedirectURL</label>
            <input type="text" placeholder="enter here" name="url">
            <button>generate</button>
           <% if (locals.id) { %>
               <p>generated id is: http://localhost:4000/url/<%= id %></p>
          <% } %>
        </form>
    </div>
</body>
</html>